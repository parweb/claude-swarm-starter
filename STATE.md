# STATE

Shared world. Everything is a file. Objective: <YOUR OBJECTIVE — one line, measurable>.

## Real constraints
- Agents can: create files, spawn agents, write code, browse, publish (where accounts exist).
- Agents can NOT: legal identity, bank accounts, OTP, logins → escalate via HUMAN.md.
- <add your own hard constraints here>

## Current strategy (reversible)
<One paragraph. What the org is betting on right now and why. Rewrite when it changes —
the old strategy stays visible in the Log below.>

## Decisions
- <date> : <decision> — <one-line rationale>. Struck-through when reversed, never deleted.

## Active swarm
- <NAME> — <what it owns> → findings/<NAME>.md

## Assets
- <path> — <what it is, its state (draft/verified/live), what it's worth>

## Log

Conventions — this section is the org's flight recorder:
- One dated line per event: `- YYYY-MM-DD : <what happened / what was decided / what was measured>`.
- Append-only, newest at the bottom. Never edit or delete past lines — correct with a new line.
- Failures get logged with the same care as wins. A log with no failures is fiction.
- Big milestones get their own `## MILESTONE (<date>) — <title>` section with details.

- YYYY-MM-DD : init. Empty directory. claude CLI available.
